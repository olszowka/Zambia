TITLE='Session Interest Counts by Participant'
DESCRIPTION='Just how many panels did each participant sign up for anyway?'
QUERY='select CongoDump.badgeid, badgename, count(sessionid) as inerested from CongoDump, ParticipantSessionInterest where CongoDump.badgeid=ParticipantSessionInterest.badgeid group by CongoDump.badgeid;'
