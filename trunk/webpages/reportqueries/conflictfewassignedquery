TITLE='Conflict Report - Sessions with under 4 people assigned'
DESCRIPTION='Not all of these are actually conflict, you want to think about them'
QUERY="select trackname, POS.sessionid, title, count(badgeid) assigned from ParticipantOnSession POS, Sessions S, Tracks T where T.trackid=S.trackid and S.sessionid=POS.sessionid group by sessionid having assigned<4 order by trackname, POS.sessionid;"
