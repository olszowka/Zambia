TITLE='Conflict Report - Assigned v Scheduled issues'
DESCRIPTION='These are sessions that are either in the grid and have no one assinged or the have people assigned and are not in the grid'
QUERY="select trackname, S.sessionid, title, if ((SCH.sessionid is NULL),'no','yes') in_grid, num_assigned from Tracks T, Sessions S left join Schedule SCH on S.sessionid=SCH.sessionid left join (select sessionid, count(badgeid) as num_assigned from ParticipantOnSession group by sessionid) A on A.sessionid=S.sessionid where T.trackid=S.trackid having (in_grid='no' and num_assigned>0) or (in_grid='yes' and (num_assigned<1 or num_assigned is NULL));"
