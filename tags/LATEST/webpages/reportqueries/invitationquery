TITLE='Invited Guest Report'
DESCRIPTION='For each invited guest session, list the participants who have been invited. (and have not deleted the invitation.) Shows "NULL" if no one has been invited.'
QUERY='SELECT T.trackname as Track, S.sessionid as "Session<BR>ID", S.title as "Title", PSI.badgeid as "BadgeId", C.lastname as "Last Name" FROM Sessions AS S JOIN Tracks AS T ON S.trackid=T.trackid LEFT JOIN ParticipantSessionInterest AS PSI ON S.sessionid=PSI.sessionid LEFT JOIN CongoDump as C on PSI.badgeid=C.badgeid WHERE T.selfselect=1 and S.invitedguest=1 and statusid=2 order by T.display_order, S.sessionid;'
