TITLE='Assigned Session by Session'
DESCRIPTION='Shows who has been assigned to each session.'
QUERY='select badgename, moderator, trackname, Sessions.sessionid as SessionId, title, statusname from ParticipantOnSession, Sessions, CongoDump, Tracks, SessionStatuses where ParticipantOnSession.badgeid=CongoDump.badgeid and ParticipantOnSession.sessionid=Sessions.sessionid and Tracks.trackid=Sessions.trackid and Sessions.statusid=SessionStatuses.statusid order by trackname, Sessions.sessionid;'

